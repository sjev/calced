[project]
name = "calced"
version = "2.0.0"
requires-python = ">=3.10"
description = "A notepad calculator that evaluates math expressions in plain text"
license = "MIT"
authors = [
    {name = "Karl Bartel", email = "karl@karl.berlin"},  # original author
    {name = "Jev Kuznetsov", email = "jev.kuznetsov@gmail.com"},
]

# No package: this file holds the version and the task runner.
[dependency-groups]
dev = ["invoke>=2.2"]
